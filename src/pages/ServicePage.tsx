import styled from "styled-components";

const ServicePage = () => {
  return <Container>ServicePage</Container>;
};

export default ServicePage;

export const Container = styled.div`
  transition: all 0.3s ease-in-out;
`;
